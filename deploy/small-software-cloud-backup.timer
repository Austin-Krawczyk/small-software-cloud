# Runs a data-dir backup once a day (and on the next boot if a run was missed).
[Unit]
Description=Daily Small Software Cloud backup

[Timer]
OnCalendar=daily
RandomizedDelaySec=15m
Persistent=true

[Install]
WantedBy=timers.target
